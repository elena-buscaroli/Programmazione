#include <stdio.h>

/*
    Scrivere una funzione che riceva in ingresso il prezzo iniziale di un articolo (di tipo float)
    e il valore percentuale di sconto (di tipo int), e restituisca il prezzo scontato.
*/

float prezzo_scontato(float prezzo_iniziale, int sconto)
{
}

int main()
{

    return 0;
}
