#include <stdio.h>

/*
    Leggere un array di 10 numeri interi e scrivere una funzione che prenda come input l'array 
    inserito, la sua dimensione e una variabile "check", inizializzata a 0 e passata tramite puntatore, 
    che sarà uguale a 0 se la sequenza inserita è palindroma, o 1 viceversa → se non cambia ad essere 
    letta dalla prima cella all'ultima o viceversa.
    Esempio: la sequenza 12321 è palindroma.
*/

int main()
{
    return 0;
}