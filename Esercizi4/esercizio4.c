#include <stdio.h>

/*
    Scrivere un programma che dato un numero naturale N, costruisca due array A e B di dimensione
    N (di un tipo numerico a scelta tra int, float oppure double) inizializzandoli da tastiera,
    e ne calcoli il prodotto scalare A*B.
    Il prodotto scalare e definito come: A*B=sum(a[[i]*b[i]), per ogni i=1,...,N.
*/

int main()
{
    return 0;
}