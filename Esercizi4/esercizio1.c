#include <stdio.h> // scanf, printf

/*
	Considerare due array statici A e B dimensione N, ed inizializzati tramite input da tastiera.
	1.  Per ogni indice i, sommare e moltiplicare A[i] e B[i] salvare i risultati in un terzo array C, all'indice i (usando lo stesso array di supporto, sovrascrivendo il valore per ogni operazione che deve essere eseguita);
	2.  confrontare gli elementi A[i] e B[i] dei due array e salvare l'elemento maggiore in C[i];
	3.  determinare la media, il valore massimo ed il valore minimo dell'array C risultante dal punto precedente. 
	Eseguire tutte le istruzioni nel main. 
*/

int main()
{
    return 0;
}
