#include <stdio.h> // scanf, printf

/*
    Considerare due array statici A e B, di stessa dimensione, ed inizializzati tramite input da tastiera.
    Implementare le seguenti istruzioni:
    1. per ogni indice, sommare e moltiplicare gli elementi dei due array e salvare i risultati in un
       terzo array C di supporto (utilizzare lo stesso array di supporto, andando a sovrascrivere per
       ogni operazione che deve essere eseguita);
    2. per ogni indice, confrontare gli elementi dei due array e salvare in un terzo array C di supporto
       l'elemento maggiore in corrispondenza dell'indice di confronto;
    3. determinare la media, il valore massimo ed il valore minimo dell'array C risultante dal punto precedente.
*/

int main()
{
    return 0;
}